[powershell://default]
*The name must be unique (and should be a valid filename)

script = <command>
*A powershell command-line script that will be executed

schedule = <schedule>
*A cron schedule for executing the script. If blank, the script will only execute once.

[powershell2://default]
*The name must be unique (and should be a valid filename)

script = <command>
*A powershell command-line script that will be executed

schedule = <schedule>
*A cron schedule for executing the script. If blank, the script will only execute once.